#!/usr/bin/python
# Generate a random number between 0 and 9

# importing the randint function from the random module
from random import randint
#Keep rerunning this program, and each time, a new number is generated within the range specified
print(randint(0,9)) #Pass in two values to the randint function, the start and end values
